import React from 'react';
import image from '../../Image/not-found.png'

const NotFound = () => {
    return (
        <div>
           <img src={image} alt="" /> 
        </div>
    );
};

export default NotFound;